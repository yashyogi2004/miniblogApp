<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
       <div class="flex justify-end"><a href="/logout" class="mb-10 bg-red-500 px-5 py-2 rounded-md ">logout</a></div>
        <h3 class="text-3xl mb-5"><span class="font-light">Hello</span>,<%= user.name %> 😊</h3>
        <h5 class="mb-5"> you can create post</h5>
        <form action="/post" method="post">
            <textarea class="w-full h-30 rounded-md outline-none p-3 bg-transparent border-2 border-zinc-800" placeholder="what's on your mind" name="content"></textarea>
             <input type="submit" value="post" class="px-5 py-2 rounded-md outline-none bg-blue-500 text-xl">
        </form>
        <div class="mt-10">
            <h3 class="text-zinc-400 mb-5">your posts</h3>
            <div class="postcontainer flex gap-5">
                <% user.posts.reverse().forEach((post) => { %>
                    <div class="post w-1/3 p-4 border-2 border-zinc-800 rounded-md">
                        <h4 class="text-blue-300 text-xl mb-2"><%=user.name%></h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small><%=post.likes.length%> likes</small>
                        <div class="btns flex gap-20">
                            <a class="text-blue-300" href="/like/<%=post._id%>">
                               <%=post.likes.indexOf(user.id) === -1 ? "like" : "unlike" %>
                            </a>
                            <a class="text-zinc-300" href="/edit/<%=post._id%>">edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>